fix: v1.2.1 - fix worklogs history showing stale data

- Change admin/superadmin default query from orderBy(date) limit(100)
  to orderBy(createdAt) limit(500) so imported worklogs appear correctly
- Update README to reflect current Firebase/Next.js stack
