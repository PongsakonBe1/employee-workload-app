fix: v1.2.2 - revert createdAt orderBy, keep orderBy date limit 500

Previous build was not deployed — production was still serving limit(100).
Reverted orderBy(createdAt) back to orderBy(date) to avoid missing index error.
