rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TEMPORARY: Allow all access (no auth required)
      // เปลี่ยนกลับเป็น request.auth != null หลังจากแก้ปัญหาเสร็จ
      allow read, write: if true;
    }
  }
}
